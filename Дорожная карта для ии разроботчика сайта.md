Дорожная карта Raven OSINT — только незакрытая работа

Правила ведения файла:
Здесь остаются только пункты, которые НЕ сделаны или сделаны частично. Как только пункт закрыт и проверен в работающем приложении — он удаляется из этого файла тем же коммитом. Никаких отметок, галочек и смайликов: если пункт написан здесь, значит он не готов.
Проверка сделанности — только по коду и по запущенному приложению, а не по записям в документах.
Полное ТЗ и требования по каждому разделу лежат в /RAVEN-SPEC (00_MASTER.md и далее). Нумерация в скобках — раздел исходного ТЗ.
Ревизия файла: 2026-08-22, после ручной проверки запущенного приложения.

---

# 1. Sherlock — проба возможностей инстанса и вотчлист (раздел 12)

Сделано: проба версии и флагов образа перед первым запуском (§3.6), экран дифа повторного запуска (§6.5), вотчлист (§6.6) — хранение вотчей, расписание с джиттером ±10%, квоты 25/проект и 100/инстанс, авто-пауза при истечении согласия через 90 дней, манифест (контейнер в песочнице рантайма, образ по digest, read-only, gVisor, приватные диапазоны запрещены), вход username из выделенного узла, запуск из карточки узла и из палитры («Run integration…»), защитный парсер трёх форм JSON с правилом «непонятный ответ — не попадание», проверка ссылок профилей на SSRF, маппинг найденных профилей в узлы и связи username -> profile, обработка отмены и частичного результата, регистрация одной строкой (без digest интеграция просто не появляется).

Не сделано: фоновое исполнение вотчей (повторяемый BullMQ-джоб), спарклайн на узле и уведомление «N изменений для @handle».

# 2. Автоматическое исследование GitHub-репозиториев (разделы 14, 37)

Сделано: интеграция github в реестре, парсер репозитория/владельца/домашней страницы/контрибьюторов, анализ репозитория в packages/domain, панель Repository Analysis в интерфейсе (структурированные факты, черновик Integration Proposal, разбор ошибок с действиями Retry / Open Repository / Analyze Manually).

Не сделано:
- запуск анализа из интерфейса (чтение и показ последнего анализа в инспекторе уже работают);
- действия на самом узле репозитория.

# 3. Глобальный поиск и палитра команд (раздел 17)

Сделано 2026-08-23: палитра снова видит открытую доску (пустой список «No commands yet» на доске по умолчанию), команды — добавить заметку, экспорт, импорт, 6 режимов холста, группы, слои, AI, Auto arrange, запуск интеграции; поиск по узлам, заметкам, тегам, проектам и доскам, тип и подпись связи ищутся по её концам; переход к найденному узлу центрирует камеру и подсвечивает карточку.

Не сделано:
- поиск по файлам и результатам запусков инструментов, и поиск сразу по всем доскам (индекс это умеет, но заполняется только текущей доской);
- выбор тега в режиме `#` ничего не фильтрует на холсте.

# 4. Plugin SDK (раздел 33)

Сделано 2026-08-23: публичный пакет `@nexus/plugin-sdk` (манифест плагина, разрешения, `definePlugin`, версия API с проверкой совместимости, события и жизненный цикл, запуск в Web Worker), README для стороннего автора, точка расширения интерфейса — команды плагина видны в Ctrl+K и выполняются в песочнице, тост от плагина подписан его именем.

Не сделано:
- панели, секции инспектора и пункты контекстного меню от плагина (нужен iframe-хост);
- собственные типы узлов и их карточки на холсте (манифест их принимает, холст — ещё нет);
- экран настроек плагина и хранилище его значений;
- `graph.propose()` — предложение узлов и связей от плагина;
- установка из реестра: сейчас плагин ставится записью в локальном хранилище браузера.

# 5. Единый запрос (P17)

packages/query-engine не создан. Спека — RAVEN-SPEC/24_UNIFIED_QUERY.md. Роутер возможностей, планировщик, бюджеты, потоковое исполнение, дедупликация, происхождение данных, режимы Local-only / Zero-credential / Free-tier / Full.

# 6. Слой transform'ов L4.2–L4.7

Каталог и реестр transform'ов есть (packages/transforms), но он не подключён к приложению. Осталось: SDK transform'ов, история и кэш запусков, реальное исполнение, UX разворачивания сущности на холсте, хранилище ключей, планирование агентом. Разделы L4 в RAVEN-SPEC/20_ROADMAP.md.

# 7. Мобильный и планшетный UX (раздел 25)

Есть базовые медиазапросы. Отдельного мобильного сценария работы с холстом нет.

# 8. Доводка перед продакшеном (раздел 47, P16)

Нагрузочная проверка на тысячах узлов на реальной машине, проверка утечек памяти, аудит доступности с клавиатуры и скринридером, финальный проход по деплою (RAVEN-SPEC/19_DEPLOYMENT.md).

---

Что уже закрыто и поэтому удалено из этого файла: разделы 1–10, 13, 15, 20–24, 26–32, 34–35, 38–46, 48–56 исходного ТЗ (холст, типы узлов, связи и маршрутизация, вставка и захват контента, файлы, автораскладка, проекты и кросс-проектные ссылки, сохранение и версии, offline-first, производительность и бенчмарки, анимации, доступность, модель данных, безопасность и песочница, импорт/экспорт и отчёт, презентация, шесть режимов представления, реестр и архитектура интеграций, стек, дизайн-система, вся документация RAVEN-SPEC, гейты качества).
