# Files that carry no diff-coverage obligation (scripts/diff-coverage.mjs).
# Every entry must name the file and the reason; they mirror the `coverage.exclude` lists in the
# package vitest configs, which vitest applies but the diff gate cannot read.
# Syntax: one repo-relative path per line, `#` starts a comment.

# Process bootstrap: binds a port and opens DB/Redis connections, so it can only be exercised by
# the e2e job (apps/api/vitest.config.ts excludes it from coverage for the same reason).
apps/api/src/server.ts

# Process bootstrap: boots a real Hocuspocus/WS listener plus DB/Redis connections. Excluded from
# vitest coverage in apps/sync/vitest.config.ts for the same reason as apps/api/src/server.ts;
# exercised by the e2e collab suite instead.
apps/sync/src/server.ts

# Better-Auth wiring: constructs the auth instance against a live Prisma client, so unit tests
# cannot import it without a database. Exercised end to end by the e2e job (J01 signup).
apps/api/src/auth/index.ts

# Pure re-export barrels (no executable statements, so v8 reports no coverage entry at all).
apps/sync/src/awareness.ts
packages/config/src/index.ts
packages/db/src/index.ts
packages/domain/src/index.ts
packages/domain/src/nodes/index.ts
packages/domain/src/edges/index.ts
packages/domain/src/edges/routing/index.ts
packages/domain/src/projection/index.ts
packages/canvas-engine/src/index.ts
packages/layout/src/index.ts
packages/transforms/src/index.ts
packages/transforms/src/catalog/index.ts
packages/transforms/src/sdk/index.ts
packages/integrations/src/index.ts
packages/ai/src/index.ts
packages/ui/src/index.ts
packages/plugin-sdk/src/index.ts

# Parser registry: re-exports plus two one-line dispatch helpers, fully exercised by
# test/github.parsers.test.ts, but vitest's global `**/index.ts` coverage exclude means v8 emits
# no entry for it at all.
packages/integrations/github/parsers/index.ts
packages/integrations/src/testkit/index.ts

# Browser bootstrap: finds #root and calls createRoot, so it can only run against a real document
# root. Everything it composes (AppProviders, AppRoutes) is covered by unit tests; the file itself
# is exercised by the e2e and visual jobs, which load the built bundle.
apps/web/src/main.tsx

# Browser-only engine boundary: the whole effect needs a real 2D canvas context, pointer capture
# and ResizeObserver, so under jsdom it returns early by design (see the guard in the file). The
# behaviour it wires up is covered by the e2e and visual jobs instead.
apps/web/src/app/canvas/useCanvasEngine.ts

# Vitest preset shared by every package: it is configuration, not shipped code, and vitest's own
# `coverage.exclude` drops `*.config.*` for the same reason. Its correctness shows up as the
# coverage floors it enforces on every other package.
packages/config/vitest/base.ts

# Worker bootstrap: binds `self.onmessage` in a DedicatedWorkerGlobalScope, so it cannot be
# imported under jsdom. The layout it runs is covered by packages/layout unit tests and the
# runner tests; the message plumbing is exercised by the e2e J06 journey.
apps/web/src/layout/layout.worker.ts

# Barrel: re-exports only, and the global vitest exclude drops `**/index.ts` from the report, so
# the diff gate would otherwise see a 0%-covered file that has no statements to cover.
packages/query-engine/src/index.ts
